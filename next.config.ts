/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com","images.unsplash.com" ], // Add your allowed image domains here
  },
};

module.exports = nextConfig;
